/// LociVault package stub. Real VaultServing implementation lands in PR04.
/// Index must never live in the vault — only Application Support.
public enum LociVaultModule {
    public static let stubVersion = "0.0.1-pr01"
}
